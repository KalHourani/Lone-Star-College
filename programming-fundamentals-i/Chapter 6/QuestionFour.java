public class QuestionFour{

     public static void main(String []args)
	 {
		 RetailItem item1 = new RetailItem("Jacket", 12, 59.95);
		 RetailItem item2 = new RetailItem("Designer Jeans", 40, 34.95);
		 RetailItem item3 = new RetailItem("Shirt", 20, 24.95);
		 /* Optional code to print descriptions, units on hand, and price of items.
		 System.out.println(item1.getDescription() + ": " + item1.getUnits() + " units on hand at $" +  item1.getPrice() + " per item.");
		 System.out.println(item2.getDescription() + ": " + item2.getUnits() + " units on hand at $" +  item2.getPrice() + " per item.");
		 System.out.println(item3.getDescription() + ": " + item3.getUnits() + " units on hand at $" +  item3.getPrice() + " per item.");
		 */
	 }
}